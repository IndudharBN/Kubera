node_modules/
build/
dist/
daemon/dist/
coverage/
.DS_Store
*.log
logs/*.log
.env*
!.env.example
!daemon/.env.daemon.example
supabase/.temp/

# Kite instrument master cache (regenerated daily)
data/kite-instruments-*.json
scripts/ls-export.json

# Runtime state — machine-local, regenerated by the daemon on each run
data/daemon-state.json
data/universe-cache.json

# IDE
.idea/

# session token (regenerates daily) + scratch backtest output
data/kite-token.json
data/bt_*.txt
